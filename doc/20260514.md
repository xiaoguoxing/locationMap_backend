All three files have been updated to align the legend categories across the dashboard bottom legend and the in-map legend, with continuous no-gap logic in the color-coding function:

### 1. [`dashboard.html`](dashboard/templates/dashboard.html:139) — Added `< 0.2` Insufficient row to Chlorine legend
- New row: `< 0.2` Insufficient (red badge, opacity 0.7) — was missing
- Existing rows unchanged: `0.2 - 1.5` Satisfactory, `1.6 - 2.0` Slightly High, `2.1 - 3.0` High, `> 3.0` Very High

### 2. [`choropleth_mapper.py`](maps/choropleth_mapper.py:321) — Updated in-map legend text in both functions

**Chlorine** (was 4 categories, now 5 matching dashboard):
- `< 0.2` (red, opacity 0.7) Insufficient
- `0.2–1.5` (green) Satisfactory
- `1.6–2.0` (orange) Slightly High *(was `1.5–2.0`)*
- `2.1–3.0` (darkred, opacity 0.7) High *(new — was merged into `>2.0`)*
- `> 3.0` (red) Very High *(was `>2.0`)*

**E. coli** (was 3 percentage tiers, now 2 binary matching dashboard):
- `0` (green) Satisfactory *(was `0%`)*
- `Any value >0` (red) Unsatisfactory *(was `≤10%` orange + `>10%` red)*

**Turbidity** (was 3 categories, now 4 matching dashboard):
- `≤ 1.5` (green) Satisfactory
- `1.6–3.0` (orange) Slightly High
- `3.1–10.0` (darkred, opacity 0.7) High *(new — was merged into `>3.0`)*
- `> 10.0` (red) Very High *(was `>3.0`)*

### 3. [`district_aggregator.py`](maps/district_aggregator.py:175) — Updated `get_color_for_district()` with no-gap continuous boundaries

**Chlorine**: `< 0.2` → red, `0.2–1.5` → green, `1.5<–2.0` → orange, `2.0<–3.0` → darkred, `> 3.0` → red

**E. coli**: `== 0` → green, `> 0` → red (removed `%`-based tiers)

**Turbidity**: `≤ 1.5` → green, `1.5<–3.0` → orange, `3.0<–10.0` → darkred, `> 10.0` → red

Note: Maps need to be regenerated (run the map generation scripts) for the in-map legend changes to take effect in the HTML output files.
